"""Pre-compiled gifsicle binary for GIF optimization.

The compiled gifsicle binary is installed directly to the Python scripts
directory by CMake/scikit-build-core. It's available as `gifsicle` in PATH
when the package is installed.
"""

from __future__ import annotations

__version__ = "1.96"
